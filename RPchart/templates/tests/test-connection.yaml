apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "RPchart.fullname" . }}-test-connection"
  labels:
    {{- include "RPchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "RPchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
