apiVersion: v1
kind: Service
metadata:
  name: {{ include "rp_chart.fullname" . }}
  labels:
    {{- include "rp_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "rp_chart.selectorLabels" . | nindent 4 }}
