apiVersion: v1
kind: Service
metadata:
  name: {{ include "RPchart.fullname" . }}
  labels:
    {{- include "RPchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "RPchart.selectorLabels" . | nindent 4 }}
